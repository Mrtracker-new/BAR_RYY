fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.20
cryptography==44.0.0
pydantic==2.10.6
pydantic-settings==2.7.1
aiofiles==24.1.0
aiosqlite==0.20.0
asyncpg==0.30.0
httpx==0.28.1
python-dotenv==1.0.1
Pillow==11.1.0
qrcode==8.0
# sib-api-v3-sdk removed — email is now sent via httpx + Brevo REST API
# (the SDK was synchronous and blocked the event loop; httpx is fully async)
